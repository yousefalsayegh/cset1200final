
import java.util.Enumeration;
import java.util.Hashtable;
import java.util.Scanner;
//Eet Class
class Eet
{
   Hashtable<String, Integer> courses = new Hashtable<String, Integer>();
   Scanner sc = new Scanner(System.in);
   String[] coursesTaken = new String[40];
   int totalCredits = 0;
   int y = 0;
   //constructor
   Eet()
   {
   	//put all classes in a hashtable Please done here 






















//ask user again to enter correct course code
   			System.out.println("Error: the class uncorect, Please try to enter a correct class.");
   			i--;
   		}
   		i++;
   	}
   }
   void result()
   {
   	if(totalCredits >= 128 )
   	{
   		System.out.println("Congratulations.");
   	}else
   	{
   		System.out.println("You have " + (128 - totalCredits) + " EET credit hours remaining.");
   		System.out.println("To complete your credit hours Please Take from following list of class");
   		Enumeration e = class.keys();
   		while(e.hasMoreElements())
   		{
   			String key = (String)e.nextElement();
   			if(Arrays.asList(classTaken).contains(key))
   			{
   			}else{
   				System.out.println(key + " : " + class.get(key));
   			}
   		}
   	}
   }
}
